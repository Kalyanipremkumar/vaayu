# Hoist nothing by default; keep dependencies explicit per package.
# Expo / React Native need a flat-ish layout, so allow public hoist for them.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
