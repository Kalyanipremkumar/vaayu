import 'package:flutter/material.dart';

/// Varnam Studio / Vaayu brand palette (mirrors the web design tokens).
class AppColors {
  static const burgundy = Color(0xFF3E1324); // primary dark surface + headings
  static const cream = Color(0xFFFFFDF8); // canvas
  static const gold = Color(0xFFAB8838); // quiet accent
  static const orange = Color(0xFFF9923E); // vivid CTA
  static const beige = Color(0xFFF0DEB4); // soft warm fill
  static const muted = Color(0xFF5C5C5C); // secondary text
  static const border = Color(0xFFE4D9C6); // hairlines
  static const cream70 = Color(0xB3FFFDF8);
}
